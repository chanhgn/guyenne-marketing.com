node_modules/
out/
.DS_Store
*.log

# public/ holds rushes (~700 MB of .mp4) which stay out of git.
# But we DO commit the small images (logo + before/after photos).
public/*
!public/*.jpg
!public/*.jpeg
!public/*.png
# Polices auto-hébergées (nécessaires au rendu hors-ligne de la présentation KOBO)
!public/fonts/
!public/fonts/*.woff2
previews/*
!previews/*.mp4
