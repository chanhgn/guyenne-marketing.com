// Google Fonts loading disabled in sandbox (SSL cert issues with fonts.gstatic.com).
// Falls back to system serif/sans/arabic stacks defined in theme.ts.
// To re-enable in production, restore the @remotion/google-fonts imports below.
//
// import { loadFont as loadCormorant } from '@remotion/google-fonts/CormorantGaramond';
// import { loadFont as loadInter } from '@remotion/google-fonts/Inter';
// import { loadFont as loadCairo } from '@remotion/google-fonts/Cairo';
// loadCormorant('normal', { weights: ['400', '500', '600'], subsets: ['latin', 'latin-ext'] });
// loadInter('normal', { weights: ['300', '400', '500', '600'], subsets: ['latin', 'latin-ext'] });
// loadCairo('normal', { weights: ['300', '400', '500', '600', '700'], subsets: ['arabic', 'latin'] });

export {};
